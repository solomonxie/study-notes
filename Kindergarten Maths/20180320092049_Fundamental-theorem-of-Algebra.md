# `Fundamental theorem of Algebra`

> If the `Polynomial` has `n` degrees, then this `polynomial` has amount of n `roots`.
But not all roots are `real number`.
And only `real number roots` intersect with `x-axis`.
